class Hamburger {
    constructor(size, stuffing) {
        this.size = size;
        this.stuffing = stuffing;
    }

    addTopping(topping) {
        let addTop = document.querySelector([type=checkbox])
        console.log(addTop);
    }

    removeTopping(topping) {

    }

    getToppings(topping) {

    }

    getSize() {
        size = document.getElementsByClassName(".size");
        
    }

    getStuffing() {

    }

    calculateCalories() {

    }

    calculatePrice() {

    }
}

const init = () => {
    
}

window.onload = init;